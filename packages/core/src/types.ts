/**
 * Re-export of core domain types inferred from Zod schemas.
 *
 * Intent: Provide a clean import path for type-only usage.
 */
export type { Board, BoardId, Card, CardId, Column, ColumnId } from './schema';
